// MISRA C-2012 Rules
{
    version : "2.0",
    standard : "c2012",
    title: "Coverity MISRA Configuration",
    deviations : [
        // Disable the following rules.
        {
	    deviation: "Directive 4.9",
            reason: "Allow inclusion of function like macros. Asserts, logging, and topic string macros use function like macros."
        },
        {
            deviation: "Rule 2.5",
            reason: "Allow unusued macros. Macros defined for topic strings are not used by the library, but are part of the API."
        }
    ]
}
